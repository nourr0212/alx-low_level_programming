#include "main.h"
/**
 * main - defines what the main purpose is
 * here is another line of command
 * Return: gives a response
 */
int main(void)
{
	int i;

	i = 0;

	positive_or_negative(i);

	return (0);
}
